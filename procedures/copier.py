#!/usr/bin/python

import os
import shutil
from glob import glob


def cmdCP(ss):
    for filename in glob(''):
        shutil.copy2(filename, '')


if __name__ == "__main__":
    for ss in subjects:
        cmdCP(ss)
